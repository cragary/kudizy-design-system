{
  "name": "static_src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:css": "node-sass -o ./css sass",
    "compile:css:watch": "node-sass -o ./css sass --watch",
    "lint:css": "sass-lint sass/** -v -q"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.3.1",
    "node-sass": "^4.13.1"
  }
}
